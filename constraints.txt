# Constraints to reduce pip version search space and avoid known conflicts
# Constrain Werkzeug to a version compatible with Flask 3.x
Werkzeug>=3.1.0
# You can add further constraints here if needed, for example:
# Flask>=3.1.2
# openai>=1.3.0
